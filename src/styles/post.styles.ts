import styled from '@emotion/styled';

export const PostWrapper = styled.div`
  /* box-shadow: 0px 8px 20px 0px rgb(0 0 0 / 20%); */
  background: #fff;
  box-shadow: 0 4px 18px 0 rgb(189 208 223 / 30%);
  border-radius: 5px;
  margin: 50px auto 20px auto;
  position: relative;
  overflow: hidden;
  width: 100%;
`;
